Retrieval-Augmented Generation (RAG) is a system architecture that combines information retrieval with text generation.

In a RAG pipeline, user queries are first converted into embeddings.
These embeddings are used to retrieve relevant documents from a vector database.

The retrieved documents are then passed as context to a large language model.
The language model generates answers grounded in the retrieved information,
reducing hallucinations and improving factual accuracy.
